# CHANGELOG — T24.1

- Fixed invalid Vercel runtime configuration.
- Removed `runtime: python3.12` from `vercel.json`.
- Kept FastAPI under `api/*.py` with a maxDuration setting.
- Added root-cause and redeploy instructions.
