# T24.3 — api_app.py Integration Patch

Add:

```python
from fastapi import Depends
from api_security import extract_bearer_token
```

Then change the protected `/api/v1/me` endpoint so it accepts:

```python
token: str = Depends(extract_bearer_token)
```

and passes `token` into the existing session/user resolver.

Example shape:

```python
@app.get("/api/v1/me", response_model=MeResponse)
def api_me(token: str = Depends(extract_bearer_token)):
    user = resolve_current_user_from_token(token)
    ...
```

Do not create a second authentication implementation. Reuse the existing
T13/T19 session resolver.

The exact function name of the current resolver must be taken from the
repository version being merged; this package intentionally does not
invent a replacement.
